final class AppConstants {
  AppConstants._();

  // Network
  static const baseUrlPath = 'https://samba.deno.dev';
  static const connectTimeout = Duration(seconds: 2);
  static const receiveTimeout = Duration(seconds: 3);
}
